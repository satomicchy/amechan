
以下のページで更新がありました。

要望
<% if @needs.present? %>
  <% @needs.each do |need| %>
    【<%= t(need.status) %>】<%= need.title %>
    <%= need.content %>
    <%= polymorphic_url need, protocol: 'https' %>

  <% end %>
<% end %>

コメント
<% if @comments.present? %>
  <% @comments.each do |comment| %>
    【<%= t(comment.need.status) %>】<%= comment.need.title %>
    <%= comment.memo %>
    <%= polymorphic_url comment.need, protocol: 'https' %>

  <% end %>
<% end %>
