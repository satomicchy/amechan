
以下のページで更新がありました。

<% if @needs.present? %>
  <% @needs.each do |need| %>
    【<%= t(need.status) %>】<%= need.title %><br />
    <%= need.content %><br />
    <%= polymorphic_url need, protocol: 'https' %>
    <br />
  <% end %>
<% end %>

<% if @comments.present? %>
  <% @comments.each do |comment| %>
    【<%= t(comment.need.status) %>】<%= comment.need.title %><br />
    <%= comment.memo %><br />
    <%= polymorphic_url comment.need, protocol: 'https' %>
    <br />
  <% end %>
<% end %>
