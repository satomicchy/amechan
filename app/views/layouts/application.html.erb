<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "YATSUHASHI Box" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <%= nav_bar brand: "YATSUHASHI Box", responsive: true do %>
      <%= menu_group pull: :right do %>
        <% if current_staff %>
          <% if current_staff.role == "admin"%><%= menu_item "スタッフ", staffs_path %><% end %>
          <%= menu_item "Log Out", destroy_staff_session_path, method: :delete %>
        <% end -%>
      <% end %>
    <% end %>

    <div class="container">
      <div class="row row-offcanvas row-offcanvas-right">
        <div class="col-xs-14 col-sm-10">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>

        <% if staff_signed_in? %>
          <div id="sidebar" class="col-xs-4 col-sm-2 sidebar-offcanvas">
            <div class="list-group">
              <%= link_to "すべて", root_path, class: "list-group-item" %>
              <%= link_to "確認中"  , on_hold_needs_path, class: "list-group-item" %>
              <%= link_to "対応待ち", waiting_needs_path, class: "list-group-item" %>
              <%= link_to "対応中"  , on_handling_needs_path, class: "list-group-item" %>
              <%= link_to "解決済"  , resolved_needs_path, class: "list-group-item" %>
              <%= link_to "却下"    , rejection_needs_path, class: "list-group-item" %>
            </div>
          </div>
        <% end %>

      </div>

      <hr>

      <footer>
        <p><%= link_to 'by @satomicchy', "https://github.com/satomicchy/amechan", html_options = {target: "_blank"} %></p>
      </footer>

    </div>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
