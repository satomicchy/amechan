<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "AmechanBox" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to 'Amechan Box', root_path, class: "brand" %>
          <div class="pull-right">
            <%= link_to 'Logout', destroy_staff_session_path, method: :delete, class: "btn" %>
          </div>
        </div>
      </div>
    </div>

    <div class="container">
        <div class="row">
          <div class="span9">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
          <div class="span3">
            <% if staff_signed_in? %>
              <div class="well sidebar-nav">
                <h3>いろいろ一覧</h3>
                <ul class="nav nav-list">
                  <li><%= link_to "対応待ち", waiting_needs_path %></li>
                  <li><%= link_to "対応中"  , on_handling_needs_path %></li>
                  <li><%= link_to "保留中"  , on_hold_needs_path %></li>
                  <li><%= link_to "解決済"  , resolved_needs_path %></li>
                  <li><%= link_to "却下"    , rejection_needs_path %></li>
                </ul>
              </div><!--/.well -->
            <% end %>
          </div><!--/span-->
        </div><!--/row-->

      <footer>
        <br />
        <p>&copy; ARC ACADEMY OSAKA</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
